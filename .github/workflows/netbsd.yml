name: NetBSD
on:
  pull_request:
    paths:
      - '.github/actions/**'
      - '.github/workflows/netbsd.yml'
  schedule:
    - cron: '22 02 * * *'
  workflow_dispatch:
    inputs:
      skip_functional_tests:
        description: 'Skip functional tests'
        required: true
        default: false
        type: boolean

concurrency:
  group: ${{ github.workflow }}${{ github.event_name != 'pull_request' && github.run_id || github.ref }}
  cancel-in-progress: true

env:
  # See https://docs.github.com/en/actions/reference/runners/github-hosted-runners#standard-github-hosted-runners-for-public-repositories.
  CI_NCPU: 4

jobs:
  netbsd-syslibs:
    name: 'NetBSD: system libs, no GUI'
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: netbsd {0}

    steps:
      - &CHECKOUT_BITCOIN_CORE
        name: Checkout Bitcoin Core repo
        uses: actions/checkout@v6
        with:
          repository: bitcoin/bitcoin

      - name: Start NetBSD VM
        uses: vmactions/netbsd-vm@v1
        with:
          mem: 12288
          prepare: |
            pkg_admin -K /usr/pkg/pkgdb fetch-pkg-vulnerabilities
            cd /usr
            cvs -danoncvs@anoncvs.NetBSD.org:/cvsroot checkout -P \
                pkgsrc/devel/pcre2 \
                pkgsrc/devel/capnproto \
                pkgsrc/archivers/bzip2 \
                pkgsrc/devel/libtool-base \
                pkgsrc/devel/pkgconf \
                pkgsrc/devel/zlib \
                pkgsrc/lang/gcc15 \
                pkgsrc/mk \
                pkgsrc/pkgtools \
                pkgsrc/security/openssl \
                pkgsrc/sysutils/install-sh/files
          run: |
            pkg_add digest libtool-base mktools cwrappers
            env -u PKG_PATH make -C /usr/pkgsrc/devel/pcre2 replace
            pkg_info > packages_before
            pkg_add cmake ninja-build git-base gcc14 pkgconf boost libevent sqlite3 zeromq python313 py313-zmq
            pkg_info > packages_after
            diff -w packages_before packages_after | grep '^>'
            # See https://github.com/bitcoin-core/libmultiprocess/pull/196.
            env -u PKG_PATH make -C /usr/pkgsrc/devel/capnproto CPPFLAGS='-DKJ_NO_EXCEPTIONS=0' install
          sync: 'rsync'
          copyback: false

      - &CHECKOUT_HELPER_ACTIONS
        name: Checkout helper actions
        uses: actions/checkout@v6
        with:
          sparse-checkout: .github/actions
          path: ci/nightly

      - name: Generate buildsystem
        run: |
          set -e
          cd ${{ github.workspace }}
          cmake -B build -G "Ninja" \
            -DCMAKE_C_COMPILER="/usr/pkg/gcc14/bin/gcc" -DCMAKE_CXX_COMPILER="/usr/pkg/gcc14/bin/g++" \
            -DWITH_ZMQ=ON \
            -DBUILD_BENCH=ON -DBUILD_FUZZ_BINARY=ON \
            -DWERROR=ON
          df -h

      - &BUILD
        name: Build
        run: |
          cd ${{ github.workspace }}
          cmake --build build

      - &CHECK_BITCOIND
        name: Check 'bitcoind' executable
        run: |
          set -e
          cd ${{ github.workspace }}
          ls -l build/bin/bitcoind
          file build/bin/bitcoind
          ldd build/bin/bitcoind
          ./build/bin/bitcoind -version

      - &RUN_TEST_SUITE
        name: Run test suite
        uses: ./ci/nightly/.github/actions/run-test-suite
        with:
          vm: netbsd
          envs: 'TMPDIR="${{ github.workspace }}"'

      - &RUN_FUNCTIONAL_TESTS
        name: Run functional tests
        if: ${{ ! inputs.skip_functional_tests }}
        env:
          # TODO: Fix the excluded test and re-enable it.
          # See https://github.com/bitcoin/bitcoin/issues/33128.
          FUNCTIONAL_TESTS_EXCLUDE: '--exclude feature_reindex_init.py'
        run: |
          cd ${{ github.workspace }}
          python3.13 build/test/functional/test_runner.py --ci --extended -j $((${{ env.CI_NCPU }} * 2)) --combinedlogslen=99999999 --tmpdirprefix . --timeout-factor=8 ${{ env.FUNCTIONAL_TESTS_EXCLUDE }}

  netbsd-depends:
    name: 'NetBSD: depends'
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: netbsd {0}

    steps:
      - *CHECKOUT_BITCOIN_CORE

      - name: Start NetBSD VM
        uses: vmactions/netbsd-vm@v1
        with:
          mem: 12288
          prepare: |
            pkg_admin -K /usr/pkg/pkgdb fetch-pkg-vulnerabilities
            cd /usr
            cvs -danoncvs@anoncvs.NetBSD.org:/cvsroot checkout -P \
                pkgsrc/devel/pcre2 \
                pkgsrc/archivers/bzip2 \
                pkgsrc/devel/libtool-base \
                pkgsrc/devel/zlib \
                pkgsrc/lang/gcc15 \
                pkgsrc/mk \
                pkgsrc/pkgtools \
                pkgsrc/sysutils/install-sh/files
          run: |
            pkg_add digest libtool-base mktools cwrappers
            env -u PKG_PATH make -C /usr/pkgsrc/devel/pcre2 replace
            pkg_info > packages_before
            pkg_add curl gmake cmake ninja-build git-base perl gcc14 pkgconf python313 py313-zmq
            pkg_info > packages_after
            diff -w packages_before packages_after | grep '^>'
          sync: 'rsync'
          copyback: false

      - *CHECKOUT_HELPER_ACTIONS

      - name: Build depends
        uses: ./ci/nightly/.github/actions/build-depends
        with:
          vm: netbsd
          options: "build_CC=/usr/pkg/gcc14/bin/gcc build_CXX=/usr/pkg/gcc14/bin/g++ CC=/usr/pkg/gcc14/bin/gcc CXX=/usr/pkg/gcc14/bin/g++"

      - name: Generate buildsystem
        run: |
          set -e
          cd ${{ github.workspace }}
          cmake -B build -G "Ninja" \
            --toolchain depends/$(./depends/config.sub $(./depends/config.guess))/toolchain.cmake \
            -DBUILD_BENCH=ON -DBUILD_FUZZ_BINARY=ON \
            -DWERROR=ON
          df -h

      - *BUILD
      - *CHECK_BITCOIND
      - *RUN_TEST_SUITE
      - *RUN_FUNCTIONAL_TESTS
