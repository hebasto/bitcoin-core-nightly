name: Manager
on:
  pull_request:
    paths:
      - '.github/workflows/manager.yml'
  schedule:
    - cron: '22 02 * * *'
  workflow_dispatch:
jobs:
  mananger:
    runs-on: ubuntu-slim
    steps:
      - run: true
